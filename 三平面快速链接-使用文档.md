# 三平面快速链接

这是一款面向 Cinema 4D 2024–2026 与 Redshift 节点材质的三平面连接工具。它可以将选中的源节点快速连接到当前材质中的全部三平面节点，也可以精确取消指定属性或一次取消当前源建立的全部三平面连接。

插件还提供“混合圆角”功能，可自动创建 Round Corners 与 Bump Blender，减少材质节点中的重复连线操作。

<p align="center">
  <img src="TriPlanarQuickLink/res/icon.png" alt="三平面快速链接图标" width="180">
</p>

## 使用方法

1. 激活需要处理的 Redshift 节点材质。

2. 在节点编辑器中只选择一个源节点。

   1. 不需要选择三平面节点，插件会自动扫描当前材质中的全部三平面节点。
   2. 如果源节点本身也是三平面节点，该节点会从目标列表中排除，避免自连接。

3. 按下为“三平面快速链接”设置的快捷键。

4. 在鼠标位置弹出的菜单中选择需要执行的操作。

   1. 选择“连接 · 缩放 / 偏移 / 旋转 / 混合量”，可连接对应属性。
   2. 选择“取消 · 缩放 / 偏移 / 旋转 / 混合量”，可精确取消对应属性。
   3. 选择“取消所有”，可一次取消当前源建立的四类三平面连接。

5. 操作成功后不会显示完成弹窗；只有无法执行时才会显示错误提示。

## 功能说明

### `连接 · 缩放`

将所选源节点的兼容输出连接到当前材质中全部三平面节点的“缩放”空闲端口。目标端口已有入线时会自动跳过，不会覆盖原连接。

### `连接 · 偏移`

将所选源节点连接到全部三平面节点的“偏移”空闲端口。端口类型不兼容时会自动跳过。

### `连接 · 旋转`

将所选源节点连接到全部三平面节点的“旋转”空闲端口。插件会在连接前检查节点关系，避免产生循环。

### `连接 · 混合量`

将所选源节点连接到全部三平面节点的“混合量”空闲端口，已有连接保持不变。

### `取消 · 属性`

“取消 · 缩放 / 偏移 / 旋转 / 混合量”只会移除当前所选源节点建立的对应连接。即使目标端口连接了其他节点，插件也不会修改这些连接。

### `取消所有`

一次检查全部三平面节点的缩放、偏移、旋转和混合量属性，并只移除来自当前所选源节点的连接。其他来源的连接保持不变。

### `混合圆角`

为直接连接到 RS Standard Material 凹凸输入的 Bump Map 自动加入 Round Corners 和 Bump Blender。相同结构已经存在时会静默跳过，不会重复嵌套。

## 混合圆角使用方法

1. 确认一个 Redshift Bump Map 节点直接连接到当前 RS Standard Material 的 Bump Input。

2. 只选择该 Bump Map 节点。

3. 按下插件快捷键并选择“混合圆角”。

4. 插件会自动完成以下操作：

   1. 创建一个 Round Corners 节点。
   2. 创建一个 Bump Blender 节点。
   3. 将原 Bump Map 接入混合器的基础输入。
   4. 将 Round Corners 接入 `Bump Input 0`。
   5. 将混合器输出重新连接到材质的 Bump Input。

## 参数说明

### `圆角半径`

自动创建的 Round Corners 节点半径固定为 `0.1`。

### `混合权重`

自动创建的 Bump Blender 会将 `Bump Weight 0` 设置为 `0.5`，用于混合原凹凸贴图与圆角效果。

## 使用规则

### `不覆盖现有连接`

执行连接时，目标属性已有入线便会跳过。插件不会为了建立新连接而移除或覆盖现有连线。

### `精确取消`

取消操作会核对连接来源，只移除当前所选源节点建立的连接，不会粗暴断开目标端口上的其他来源。

### `循环保护`

如果所选源节点位于目标三平面的下游，新连接会形成节点循环，插件将拒绝执行并显示提示。此时应改选位于三平面上游的数值、纹理或其他源节点。

### `处理范围`

插件只处理当前活动 Redshift 材质中的三平面节点，不会扫描或修改其他材质。

## 安装方法

1. 完全退出 Cinema 4D。
2. 解压 `TriPlanarQuickLink-C4D2024-2026.zip`。
3. 将完整的 `TriPlanarQuickLink` 文件夹复制到 Cinema 4D 的 `plugins` 文件夹。
4. 重新启动 Cinema 4D。
5. 打开“窗口 > 自定义 > 命令管理器”。
6. 搜索“三平面快速链接”或“TriPlanar Quick Link”。
7. 为该命令分配快捷键并保存快捷键布局。

正确的插件目录结构如下：

```text
plugins
└─ TriPlanarQuickLink
   ├─ TriPlanarQuickLink.pyp
   ├─ triplanar_core.py
   ├─ README.txt
   └─ res
```

如果命令管理器能够搜索到该命令并显示图标，说明插件已成功注册。

## 常见问题

### `命令管理器搜索不到插件`

确认复制的是完整插件文件夹，并检查是否出现 `TriPlanarQuickLink\TriPlanarQuickLink\...` 的重复嵌套。安装或更新后必须完全退出并重新启动 Cinema 4D。

### `执行连接后没有变化`

目标端口可能已有入线、端口类型可能不兼容，或当前材质中没有三平面节点。若所选源位于三平面下游，插件也会阻止形成循环的连接。

### `执行取消后没有变化`

取消操作只识别当前所选源节点建立的连接。如果目标连接来自其他节点，插件不会修改它。

### `混合圆角无法执行`

确认所选节点是 Redshift Bump Map，并且它直接连接到一个 RS Standard Material 的 Bump Input。经过其他中间节点的连接不符合自动混合条件。

## 更新与卸载

更新插件时，请先完全退出 Cinema 4D，删除旧的 `TriPlanarQuickLink` 文件夹，再复制新版完整文件夹。不要只覆盖部分文件，以免 Cinema 4D 继续加载旧代码。

卸载时，完全退出 Cinema 4D 后删除 `plugins` 目录中的 `TriPlanarQuickLink` 文件夹即可。卸载插件不会修改已经保存在材质中的节点和连线。
